[package]
name = "server"
version = "0.0.1"
edition = "2021"

[lib]
name = "server"
path = "src/lib.rs"

[dependencies]
axum = "0.6.18"
engine = { path = "../engine" }
serde = "1.0.163"
tokio = { version = "1.28.1", features = ["full"] }
