[package]
name = "server"
version = "0.0.1"
edition = "2021"

[lib]
name = "server"
path = "src/lib.rs"

[dependencies]
axum = "0.6.11"
engine = { path = "../engine" }
serde = "1.0.147"
tokio = { version = "1.26.0", features = ["full"] }
